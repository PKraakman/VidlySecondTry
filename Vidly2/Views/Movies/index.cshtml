@model Vidly2.ViewModels.MoviesIndexViewModel

<div>
    <h2>Movies <a href="/movies/new" class="btn btn-primary">new movie</a></h2>

    
    
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Movie</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var movie in Model.Movies)
            {
                <tr>
                    <td>@Html.ActionLink(movie.Name,"Edit", new { Id = movie.Id }) </td>
                    <td>@movie.Genre.Name</td>
                </tr>
            }
        </tbody>
    </table>
</div>

